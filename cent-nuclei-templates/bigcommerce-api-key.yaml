id: bigcommerce-api-key

info:
  name: BigCommerce API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^bcapi_[a-zA-Z0-9-_]{64}$"
