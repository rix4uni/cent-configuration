id: azure-subscription-key

info:
  name: Azure Subscription Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "azr_[0-9a-zA-Z]{32}"
