id: docusign-api-key

info:
  name: DocuSign API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^[a-zA-Z0-9-_]{40}$"
