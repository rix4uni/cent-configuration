id: fastly-api-key

info:
  name: Fastly API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "fastly_[a-f0-9]{32}"
