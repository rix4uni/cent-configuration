id: shippo-api-key

info:
  name: Shippo API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^shippo_[a-zA-Z0-9-_]{40}$"
