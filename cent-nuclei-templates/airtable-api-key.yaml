id: airtable-api-key

info:
  name: Airtable API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "pat[A-Za-z0-9]{18}"
