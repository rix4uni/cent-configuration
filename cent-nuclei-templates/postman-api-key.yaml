id: postman-api-key

info:
  name: Postman API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "PMAK-[a-f0-9]{24}-[a-f0-9]{32}"
