id: segment-api-key

info:
  name: Segment API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^seg_[a-zA-Z0-9]{32}$"
