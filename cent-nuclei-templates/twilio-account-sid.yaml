id: twilio-account-sid

info:
  name: Twilio Account SID
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "AC[a-zA-Z0-9]{32}"
