id: gitlab-access-token

info:
  name: GitLab Access Token
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "glpat-[a-zA-Z0-9-_]{20}"
