id: harvest-api-key

info:
  name: Harvest API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^harvest_[a-zA-Z0-9-_]{64}$"
