id: hubspot-api-key

info:
  name: HubSpot API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^[0-9a-f]{40}$"
