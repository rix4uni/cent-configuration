id: sendfox-api-key

info:
  name: SendFox API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "^sendfox_[a-zA-Z0-9-_]{32}$"
