id: dropbox-api-key

info:
  name: Dropbox API Key
  author: YoruYagami
  severity: high

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "sl\\.[a-zA-Z0-9]{15}"
